export abstract class BaseEntity {
  // public MapToDb(entity : Entity) : DbEntity | DbEntity[] {
  //   return CaseSerializer.CastToSnake(entity)
  // }

  // public MapFromDb(dbModel : DbEntity | DbEntity[]) : Entity | Entity[] {
  //   return CaseSerializer.CastToCamel(dbModel)
  // }
}
